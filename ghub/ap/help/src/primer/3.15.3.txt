local start_pt = nil;
function on_lbuttondown(scene,flags,x,y)
	if start_pt then
		start_pt = {x,y};
		local x,y,z = client_2_world(scene,x,y);
		start_pt = {x=x,y=y,z=z};
	else 
		scene_select(scene,x,y,1,1,1);
	end
end